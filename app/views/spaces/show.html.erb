<div class="conatiner d-flex justify-content-center align-items-center full-page">
    <div class="card">
      <div class="card-body">
        <h2 class="card-title"><%= @space.name %></h2><br>
        <h4 class="card-subtitle"><%= @space.description %></h4><br>
        <p class="card-text"><%= @space.address %></p>
        <p class=<%= @space.availability ? ' available' : ' unavailable' %>>Available</p>
      </div>
      <div class="card-footer d-flex justify-content-between">
        <p>Space rated: <%= @space.rating %></p>
        <p><%= @space.price %>€</p>
      </div>
    </div>
</div>
